<header id="header_main">
    <nav id="nav_main" class="nav">
        <div id="main-links">
            {{#if isLoggedIn}}
            <a class="link" href="/home">Home</a>
            <a class="link" href="/all-spots">Spots</a>
            <a class="link" href="/favorites">Favorites</a>
            {{/if}}
        </div>
        <div id="log">
            {{#unless isLoggedIn}}
            <a class="link" href="/signin">Login</a>
            {{/unless}}
            {{#if isLoggedIn}}
            <a class="link" href="/logout" id="nav_logout">Log out</a>
            {{/if}}
        </div>
    </nav>
</header>